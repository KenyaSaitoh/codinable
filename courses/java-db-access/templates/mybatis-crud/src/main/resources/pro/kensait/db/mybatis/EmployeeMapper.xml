<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pro.kensait.db.mybatis.EmployeeMapper">
  <resultMap id="employeeMap" type="pro.kensait.db.mybatis.Employee">
    <id property="id" column="EMPLOYEE_ID"/>
    <result property="departmentId" column="DEPARTMENT_ID"/>
    <result property="name" column="EMPLOYEE_NAME"/>
    <result property="salary" column="SALARY"/>
  </resultMap>

  <insert id="insert">
    INSERT INTO EMPLOYEE (EMPLOYEE_ID, DEPARTMENT_ID, EMPLOYEE_NAME, SALARY)
    VALUES (#{id}, #{departmentId}, #{name}, #{salary})
  </insert>

  <select id="findById" resultMap="employeeMap">
    SELECT EMPLOYEE_ID, DEPARTMENT_ID, EMPLOYEE_NAME, SALARY
    FROM EMPLOYEE
    WHERE EMPLOYEE_ID = #{id}
  </select>

  <update id="update">
    UPDATE EMPLOYEE
    SET DEPARTMENT_ID = #{departmentId}, EMPLOYEE_NAME = #{name}, SALARY = #{salary}
    WHERE EMPLOYEE_ID = #{id}
  </update>

  <delete id="delete">
    DELETE FROM EMPLOYEE WHERE EMPLOYEE_ID = #{id}
  </delete>
</mapper>
